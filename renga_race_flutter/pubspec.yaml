name: renga_race_flutter
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # FORZIAMO L'ULTIMA VERSIONE STABILE (Quella che funziona)
  permission_handler: ^11.3.1
  wakelock_plus: ^1.2.8
  share_plus: ^10.0.0
  image_picker: ^1.1.2
  path_provider: ^2.1.3
  webview_flutter: ^4.13.1
  webview_flutter_android: ^4.10.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
